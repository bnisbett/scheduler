<%= form_for(@supporter) do |f| %>
  <% if @supporter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supporter.errors.count, "error") %> prohibited this supporter from being saved:</h2>

      <ul>
      <% @supporter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :phone %><br />
    <%= f.text_field :phone %>
  </div>
	<div class="field">
  	<%= f.label :role, 'IT/Developer' %><br />
		<% if current_user.login =="dev" %>
			<%= f.select :role, [["Developer", "Developer"]] %>
		<% else %>
			<%= f.select :role, [["IT Personnel", "IT Personnel"]] %>  	
		<% end %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
